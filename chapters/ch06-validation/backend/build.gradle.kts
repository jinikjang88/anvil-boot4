// ch06-validation — 검증과 예외 — Bean Validation + Problem Details (RFC 7807)
// 공통 컨벤션만 적용. 챕터별 추가 의존성은 아래 dependencies 에 추가.
plugins {
    id("anvil.spring-boot-conventions")
}

description = "ch06-validation — 검증과 예외 — Bean Validation + Problem Details (RFC 7807)"
