// ch04-config — 설정의 기술 — local/dev/prod와 시크릿 분리
// 공통 컨벤션만 적용. 챕터별 추가 의존성은 아래 dependencies 에 추가.
plugins {
    id("anvil.spring-boot-conventions")
}

description = "ch04-config — 설정의 기술 — local/dev/prod와 시크릿 분리"
