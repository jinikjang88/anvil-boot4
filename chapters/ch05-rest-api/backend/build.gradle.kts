// ch05-rest-api — REST API 설계 — 메뉴판처럼 만들기
// 공통 컨벤션만 적용. 챕터별 추가 의존성은 아래 dependencies 에 추가.
plugins {
    id("anvil.spring-boot-conventions")
}

description = "ch05-rest-api — REST API 설계 — 메뉴판처럼 만들기"
