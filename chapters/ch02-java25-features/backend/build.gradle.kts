// ch02-java25-features — Java 25 신문법 한 입씩
// 공통 컨벤션만 적용. 챕터별 추가 의존성은 아래 dependencies 에 추가.
plugins {
    id("anvil.spring-boot-conventions")
}

description = "ch02-java25-features — Java 25 신문법 한 입씩"
