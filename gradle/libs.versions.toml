[versions]
# JVM / 빌드
java = "25"
springBoot = "4.0.0"
springDependencyManagement = "1.1.7"
foojayResolver = "0.10.0"

# 테스트
junit = "5.11.4"
assertj = "3.26.3"
testcontainers = "1.21.3"

# 인프라
postgresql = "42.7.4"

# 보안
jjwt = "0.12.6"

# 쿼리 DSL — Boot 4 / Jakarta EE 9+ 라 :jakarta classifier 필수
querydsl = "5.1.0"

# jOOQ codegen 플러그인 (ch09b optional)
jooqPlugin = "9.0"

[libraries]
# Spring Boot 의존성 (버전은 Boot BOM 이 관리하므로 명시하지 않음)
spring-boot-starter-web = { module = "org.springframework.boot:spring-boot-starter-web" }
# Jackson — Boot 4 에서 starter-web 의 transitive 로는 jackson-databind 가 컴파일 클래스패스에 들어오지 않을 수 있어 명시 의존
spring-boot-starter-json = { module = "org.springframework.boot:spring-boot-starter-json" }
spring-boot-starter-actuator = { module = "org.springframework.boot:spring-boot-starter-actuator" }
spring-boot-starter-validation = { module = "org.springframework.boot:spring-boot-starter-validation" }
spring-boot-starter-security = { module = "org.springframework.boot:spring-boot-starter-security" }
spring-boot-starter-test = { module = "org.springframework.boot:spring-boot-starter-test" }
# Boot 4 에서 WebMvcTest 슬라이스 테스트는 별도 모듈로 분리됨
spring-boot-webmvc-test = { module = "org.springframework.boot:spring-boot-webmvc-test" }
# Security 테스트 헬퍼 (@WithMockUser 등)
spring-security-test = { module = "org.springframework.security:spring-security-test" }

# JWT (jjwt 0.12 — JSON Web Token 발급/검증)
jjwt-api     = { module = "io.jsonwebtoken:jjwt-api",     version.ref = "jjwt" }
jjwt-impl    = { module = "io.jsonwebtoken:jjwt-impl",    version.ref = "jjwt" }
jjwt-jackson = { module = "io.jsonwebtoken:jjwt-jackson", version.ref = "jjwt" }

# 테스트
junit-bom = { module = "org.junit:junit-bom", version.ref = "junit" }
assertj-core = { module = "org.assertj:assertj-core", version.ref = "assertj" }
testcontainers-bom = { module = "org.testcontainers:testcontainers-bom", version.ref = "testcontainers" }
testcontainers-junit = { module = "org.testcontainers:junit-jupiter" }
testcontainers-postgresql = { module = "org.testcontainers:postgresql" }

# 드라이버
postgresql-driver = { module = "org.postgresql:postgresql", version.ref = "postgresql" }

# JPA / Hibernate (버전은 Spring Boot BOM 이 관리)
spring-boot-starter-data-jpa = { module = "org.springframework.boot:spring-boot-starter-data-jpa" }

# jOOQ — SQL-First (ch09b 에서 사용). 버전은 Spring Boot BOM 이 관리.
# Lite 워크플로 (codegen 없이) → starter 만으로 DSLContext 자동 설정.
spring-boot-starter-jooq = { module = "org.springframework.boot:spring-boot-starter-jooq" }

# QueryDSL — 타입 세이프 동적 쿼리 빌더
# - jpa: 런타임 (JPAQueryFactory)
# - apt: Q클래스 코드 생성 (annotationProcessor)
# Jakarta 네임스페이스 (Hibernate 6 / Boot 3+) 라 :jakarta classifier 필수.
# 안 붙이면 javax.persistence.* 를 import 한 Q클래스가 생성돼 컴파일 실패.
querydsl-jpa = { module = "com.querydsl:querydsl-jpa", version.ref = "querydsl" }
querydsl-apt = { module = "com.querydsl:querydsl-apt", version.ref = "querydsl" }

[plugins]
spring-boot = { id = "org.springframework.boot", version.ref = "springBoot" }
spring-dependency-management = { id = "io.spring.dependency-management", version.ref = "springDependencyManagement" }
foojay-resolver-convention = { id = "org.gradle.toolchains.foojay-resolver-convention", version.ref = "foojayResolver" }
jooq-codegen = { id = "nu.studer.jooq", version.ref = "jooqPlugin" }
